package com.kou.base.myselfUtils.steam;

import java.util.ArrayList;
import java.util.TreeSet;
import java.util.stream.Collectors;

public class LamdaAll {

//ArrayList<StoreSelectVo> res = storeSelectVos.getRecords().stream().collect(Collectors.collectingAndThen(Collectors.toCollection(() -> new TreeSet<>(Comparator.comparing(StoreSelectVo::getStoreId))), ArrayList::new));

}
